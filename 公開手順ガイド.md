# 現場のお弁当注文サイト 公開ガイド（初めての方向け）

プログラミングの知識がなくても大丈夫です。ブラウザの操作だけで、実際に現場のみんなが使えるサイトとして公開できます。所要時間は20〜30分程度です。

全体の流れは3ステップです。

1. GitHub（コードを置く場所）にファイルを置く
2. Railway（サイトを動かすサービス）で公開する
3. 実際に使う準備をする（パスワード・名前・メニュー登録）

---

## 事前に用意するもの

- Windowsパソコン
- メールアドレス
- お渡しした `bento-order-app-src.zip` ファイル

---

## ステップ1：GitHubにファイルを置く

GitHubは、コードを保管しておく場所です。無料で使えます。

### 1-1. GitHubアカウントを作る

1. https://github.com にアクセス
2. 右上の「Sign up」からアカウントを作成（メールアドレス・パスワードを設定）
3. 届いた確認メールのリンクをクリックして認証を完了する

### 1-2. 新しいリポジトリ（保管場所）を作る

1. ログイン後、右上の「＋」ボタン→「New repository」をクリック
2. Repository name に `bento-order-app` などわかりやすい名前を入力
3. 「Private」を選択（社内データなので非公開にします）
4. 一番下の「Create repository」をクリック

### 1-3. zipファイルを展開する

1. お渡しした `bento-order-app-src.zip` を、パソコンの好きな場所（デスクトップなど）に保存
2. 右クリック →「すべて展開」を選び、展開する
3. 展開すると `bento-order-app` というフォルダができます。**このフォルダを開いてください**（中身が見える状態にする）

### 1-4. GitHubにアップロードする

1. 1-2で作ったリポジトリの画面で「uploading an existing file」または「Add file」→「Upload files」をクリック
2. **重要**：`bento-order-app` フォルダそのものではなく、**フォルダを開いた中にある全部のファイル・フォルダ**（`app`、`components`、`lib`、`package.json` など）を選択して、アップロード画面にドラッグ＆ドロップする
   - フォルダごとドラッグするとファイルの場所がずれて動かなくなるので注意してください
3. 下までスクロールし、「Commit changes」（緑のボタン）をクリック

これでコードの準備は完了です。

---

## ステップ2：Railwayで公開する

RailwayはWebサイトを実際にインターネット上で動かしてくれるサービスです。無料ではなく、小規模なこのサイトであれば月に数百円〜1,000円程度の従量課金になります（正式な金額は https://railway.com/pricing でご確認ください）。

### 2-1. Railwayアカウントを作る

1. https://railway.app にアクセス
2. 「Login」→「Login with GitHub」を選び、先ほどのGitHubアカウントでログイン（連携を許可する）

### 2-2. プロジェクトを作る

1. ダッシュボードの「New Project」をクリック
2. 「Deploy from GitHub repo」を選択
3. 先ほど作った `bento-order-app` リポジトリを選ぶ
4. 自動でビルドが始まりますが、この時点ではまだ正しく動きません（次の設定が必要です）。そのまま進めてください

### 2-3. 環境変数（パスワードなどの設定）を入力する

1. 画面に表示されたサービス（四角いボックス）をクリック
2. 「Variables」タブを開く
3. 「+ New Variable」で、以下を1つずつ追加する

| 変数名 | 値 |
|---|---|
| `ADMIN_PASSWORD` | 管理画面用の好きなパスワード（例：現場の名前+数字など） |
| `SESSION_SECRET` | 適当な長いランダム文字列（英数字を30文字くらい適当に打ち込めばOK） |
| `DATA_DIR` | `/app/data` （そのまま入力） |

### 2-4. データ保存用のボリュームを追加する（一番大事な設定です）

これを忘れると、注文データや画像がリセットされてしまいます。

1. サービス画面の「Volumes」タブ（または「Settings」の中）を開く
2. 「+ New Volume」をクリック
3. Mount Path（マウントパス）に `/app/data` と入力して作成
   - 2-3で入力した `DATA_DIR` と**必ず同じパス**にしてください

### 2-5. 公開URLを発行する

1. 「Settings」タブ→「Networking」を開く
2. 「Generate Domain」をクリックすると、`〇〇.up.railway.app` のようなURLが発行されます

### 2-6. 再デプロイする

環境変数とボリュームを設定したら、サービス画面上部の「Deploy」（再デプロイ）を押して数分待ちます。緑色のチェックが付けば公開完了です。

### 2-7. 動作確認

発行されたURLをスマホのブラウザで開き、トップページが表示されるか確認してください。

---

## ステップ3：実際に使う準備をする

1. サイトの右上「管理者ログイン」→ ステップ2-3で決めた `ADMIN_PASSWORD` でログイン
2. 「名前」タブ：サンプルの名前を削除し、現場のメンバーの名前を登録
3. 「メニュー」タブ：サンプルのメニューを削除し、実際のお弁当メニューを登録
4. 「画像」タブ：今週のメニュー表の写真をアップロード
5. 発行されたURLをLINEなどでメンバーに共有

### スマホのホーム画面に追加すると便利です

- **iPhone（Safari）**：URLを開く→下の共有ボタン→「ホーム画面に追加」
- **Android（Chrome）**：URLを開く→右上の「⋮」→「ホーム画面に追加」

アプリのアイコンのようにワンタップで開けるようになります。

---

## 困ったときは

| 症状 | 確認すること |
|---|---|
| デプロイが失敗する | Variablesの3つ（`ADMIN_PASSWORD` / `SESSION_SECRET` / `DATA_DIR`）が正しく入っているか |
| 画像や注文データが消える | `DATA_DIR` の値とVolumeのMount Pathが、両方とも `/app/data` になっているか |
| 管理者パスワードを変えたい | RailwayのVariablesで `ADMIN_PASSWORD` を書き換えて「Deploy」を押し直す |
| メニューや名前を追加・修正したい | 管理画面（`/admin`）から自由に編集できます。再デプロイは不要です |

作業を進めていて分からないところが出てきたら、どこで止まっているか教えてください。
