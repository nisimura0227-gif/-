# 品質チェックレポート（現場めし・支払い方法変更＋公式LINE連携）

実施日：2026-08-09
想定：建設現場で約70人が毎日利用する運用

このレポートは、今回実装した機能（現金ケース払いの廃止、明日の注文の前日払い化、支払い確認の3段階管理、公式LINEでの通知・確認機能）について、実際の現場運用を想定して粗探しを行った結果です。

## 凡例

- **重要度 S**：金銭・データ消失・セキュリティに直結する重大な問題
- **重要度 A**：日常運用で高い頻度・確率で発生しうる、対応優先度が高い問題
- **重要度 B**：稀に発生するが実害は限定的、または回避策がある問題
- **重要度 C**：軽微・UI/UX上の改善余地

「対応済み」と書いてあるものは、このレポートを作成する過程で発見し、その場でコードを修正したものです。それ以外は運用上の注意点、または今後の拡張候補として記載しています。

---

## 1. 注文・二重注文・変更・キャンセル

**1.1 【S】〈対応済み〉Redis運用時の同時書き込みでの注文消失**
問題：本番想定のRedisバックエンドには元々ロック機構がなく、複数人がほぼ同時に注文すると、片方の注文が後勝ちで上書きされ消える可能性があった。
改善：`lib/backends/redisStore.ts` に `SET NX` による簡易ロックを追加し、注文・名前・メニュー・設定・LINE紐付けの読み取り→書き込み区間を排他制御するよう修正済み。

**1.2 【A】〈対応済み〉名前の表記ゆれによる二重注文**
問題：「注文済みかどうか」の判定（`findOrder`）は前後空白除去・大文字小文字を無視する一方、実際に保存する`upsertOrder`側は完全一致判定だった。同じ人のつもりでも表記が微妙に違うと別の注文として二重登録される不整合バグがあった。
改善：両者の判定ロジックを統一済み。

**1.3 【S】支払い済みの注文を本人がキャンセルできてしまう**
問題：現金を渡した後（`paid`状態）でも、締切前なら本人が「注文をキャンセルする」を押せてしまい、現金だけ手元に残り注文記録が消える不整合が起こり得る。
改善案：`paid`/`claimed`状態でのキャンセル時に追加の警告文言を出す、または管理者への通知を必須にする（今回は仕様未確定のため未実装。優先度の高い次回対応候補）。

**1.4 【A】キャンセルは記録が残らない完全削除**
問題：自己キャンセルは注文レコードを完全削除するため、後から「いつ・誰がキャンセルしたか」を追跡できない。
改善案：削除ではなく「キャンセル済み」ステータスとして残す設計も考えられる（既存の管理者削除機能の挙動に合わせたため今回は完全削除で統一）。

**1.5 【B】二重タップでの多重送信**
問題：通信が遅い環境で「注文する」を連打すると複数回POSTされる可能性。
改善：`upsertOrder`は同一人物・同一日付なら上書きするため実害はないが、新規注文のLINE通知が余分に届く可能性がある。送信中はボタンが無効化されるため大半のケースは防止済み。

**1.6 【B】名前を打ち替えて送信すると元の注文が残る**
問題：「変更」画面で名前欄自体を別人名に書き換えて送信すると、元の名前の注文はそのまま残り、新しい名前で別注文が作られる。
改善案：名前変更時に元の注文がどうなるかの説明表示があるとよい（管理画面から手動削除は可能）。

**1.7 【A】共用端末での名前の残留**
問題：スマホを複数人で共用する現場では、`localStorage`に保存された前回の名前が残り、別の人がそのまま注文してしまう可能性。
改善案：システム的な完全防止にはアカウント制が必要（今回は対象外）。名前欄は毎回表示され「変更」で気づけるため、都度確認する運用を推奨。

**1.8 【C】「変更」を押すだけで注文時刻が更新される**
問題：内容を変えずに「変更」→再送信しても`orderedAt`が更新され、一覧の並び順が変わる。実害は小さい。

**1.9 【B】メニュー0件時の文言が今日/明日で同一**
問題：「メニューが登録されていません」の文言が今日・明日共通。見出しで日付は分かるため実害は小さい。

**1.10 【C】大盛りチェックの引き継ぎ**
問題：メニュー変更時に大盛りチェックの外し忘れが起こり得るが、合計金額がリアルタイム表示されるため気づきやすい。

**1.11 【B】キャンセル確認ダイアログがLINEアプリ内ブラウザで崩れる可能性**
問題：`window.confirm()`はLINEアプリ内ブラウザ（LIFF）で見た目が簡素/小さくなる場合がある。実機確認を推奨。

**1.12 【C】大盛り追加料金の変更は既存注文に影響しない**
確認：仕様通り、価格変更後も過去注文の金額はスナップショットのまま変わらない（正しい挙動）。

---

## 2. 締切時間・明日の注文

**2.1 【S】締切時刻の設定変更とCron実行時刻が連動しない**
問題：管理画面で「明日の注文の締切時刻」を変更しても、Vercel Cron（`vercel.json`）の定時通知の実行時刻（6:30/15:00固定）は自動で追従しない。締切を動かした場合はサマリー通知が実態とずれる。
改善案：締切時刻を恒常的に変更する場合は`vercel.json`の`schedule`も合わせて変更し再デプロイする必要がある旨をREADME・設定手順書に明記済み。将来的な自動連動化は別途の実装が必要。

**2.2 【A】明日の注文の締切が近いことを知らせる通知がない**
問題：毎朝6:30のリマインドは「今日の注文」のみに言及し、明日分の締切（本日15:00）には触れていない。
改善案：必要であれば午後にもう一段階リマインドを追加できる（今回は要件になかったため未実装）。

**2.3 【C】締切境界での表示ズレ（最大59秒）**
問題：`useCountdown`は1分間隔更新のため、締切直後でも一瞬フォームが操作可能に見えることがある。実際の送信可否はサーバー側で必ず再チェックされるため実害はない。

**2.4 【B】深夜0時をまたぐ「本日」表記の分かりにくさ**
問題：日付が変わるタイミングで「本日15:00まで」の「本日」が指す日が切り替わる。計算自体は正しいため実害は小さい。

**2.5 【A】極端な締切時刻設定の検証不足**
問題：0:00や23:59など極端な値での動作は今回重点的に検証していない。`normalizeSettings`で0-23時・0-59分にクランプされるため異常値そのものは防止されている。

**2.6 【B】今日/明日の締切設定の混同**
問題：担当者が2つの締切設定欄を混同して逆に設定してしまう可能性。ラベルを明確に分けて表示済み。

**2.7 【S】休工決定時の返金対応はシステム外**
問題：前日払い後に休工が決まった場合の返金・キャンセル対応はシステムでは扱えず人力になる。
改善案：休工が決まった際は管理者がLINE公式アカウントから一斉連絡するフローを運用ルールとして用意することを推奨。

**2.8 【C】明日の注文一覧に支払い方法列が非表示**
問題：今日・明日とも支払い方法が「手渡し」に統一されたため、表示有無を揃えた方が一貫性がある（現状は今日のみ表示）。軽微。

**2.9 【B】サマリー通知と実際の一覧のタイミングのズレ**
問題：締切ちょうどの駆け込み注文がサマリー集計に含まれるかどうかは数十秒単位でずれる可能性。最終的にはサイト側が正のため実害は軽微。

**2.10 【C】締切後の再開案内がない**
問題：「明日の受付は終了しました」の画面に、次はいつ頼めるか（＝翌朝から今日扱いで）の案内がない。

---

## 3. 支払いトラブル・「渡した／受け取っていない」問題・支払い済みの誤操作

**3.1 【S】〈設計の核心・実装済み〉本人申告だけでは支払い済みにならない**
確認：`claimOrderPaid`は`unpaid→claimed`にしか進めず、`claimed→paid`への遷移は管理者権限を通った`setOrderPaymentStatus`経由でのみ発生する。「渡したと言っただけでは確定しない」という要件を型・実装の両方で保証。

**3.2 【A】管理者が受け取り前に確認ボタンを押してしまう可能性**
問題：ヒューマンエラーとして、現金受領前に「受け取り確認」を押してしまうケース。
改善：メッセージ本文に「実際に受け取ってから押してください」と明記済み。誤操作時は管理画面から`unpaid`に取り消し可能。

**3.3 【B】LINE側に取り消しボタンがない**
問題：管理者がLINEの「受け取り確認」を誤って押しても、LINE上で取り消す手段はなく、サイトの管理画面でのみ取り消せる。スマホでLINEしか開かない管理者だと気づきにくい。

**3.4 【S】〈対応済み〉金額変更時に支払い済み状態がそのまま残る**
問題：支払い確定後に注文内容を編集して金額が変わっても、支払い状況は（意図的な仕様として）引き継がれるため、差額の未回収に気づきにくい。
改善：注文編集時に金額が変わり、かつ支払い状況が`claimed`/`paid`だった場合、管理者へのLINE通知に「⚠️金額が変わりました。支払い状況は引き継がれています」という注意書きを追加済み。

**3.5 【A】現金の受け渡し自体はシステムで検証できない**
確認：最終的には人同士の信頼と「本人申告→管理者確認」の2段階チェックに依存する設計であることを運用担当者に理解してもらう必要がある。

**3.6 【S】〈対応済み〉客観的な記録としての申告時刻が管理画面に見えない**
問題：「渡した」「受け取っていない」の水掛け論が起きた場合、`paymentClaimedAt`（申告時刻）・`paymentConfirmedAt`（確認時刻）が唯一の客観的記録になるが、管理画面の注文一覧にこれらの時刻が表示されていなかった。
改善：`components/OrderListSection.tsx` の各注文カードに「申告 HH:MM／確認 HH:MM」の表示を追加済み。

**3.7 【A】取り消し操作は本人に通知されない**
問題：管理者が`paid→unpaid`に取り消しても、本人には何も通知が飛ばない（意図的な設計：誤操作の是正が目的のため）。頻発するようなら再通知の検討余地あり。

**3.8 【B】連打しても二重処理されないが、フィードバックが薄い**
確認：「渡しました」ボタンは連打しても`claimOrderPaid`が冪等なため実害はないが、押した後の反応が控えめで不安に感じるユーザーがいる可能性。

**3.9 【C】支払い状況バッジは絵文字＋テキストで色弱の方にも配慮**
確認：⏳🙋✅の絵文字だけでなくテキストラベルも併記されており、色だけに依存しない設計になっている。

**3.10 【B】通知メッセージに日付情報が一部省略**
問題：支払い確認等のLINE通知本文に「何日分の注文か」の日付が省略されている箇所がある。複数日分が同時に絡むケースは稀だが明記した方が親切。

**3.11 【C】支払い方法「手渡し」固定化による将来の拡張性**
確認：`PAYMENT_METHOD`定数で一元管理されているため、将来別の支払い手段を追加する場合も変更箇所は限定的。

**3.12 【S】支払い確定権限の実質的な境界はパスワード＋LINE登録リストの管理次第**
確認：支払いを`paid`にできるのは「管理画面ログインできる人」全員、または「`adminLineUsers`に登録された人」全員。これらのリスト管理が実質的なセキュリティ境界となるため、パスワードの使い回し・退職者の未削除に注意（4章・10章で詳述）。

**3.13 【A】大盛り追加料金変更後も過去注文の金額は不変**
確認：`unitPrice`/`largeExtra`を注文時点でスナップショット保存する既存設計により、金額の食い違いは起きない（正しい挙動）。

**3.14 【B】「渡しました」を押すタイミングは性善説に依存**
確認：技術的には現金を渡す前でも押せてしまうが、最終確定は管理者確認に一本化されているため実害はない。

---

## 4. 管理者の確認忘れ・管理者が休みの場合・権限管理

**4.1 【S】〈対応済み〉管理者が1人だと不在時に誰も確認できない**
改善：管理者LINE通知先を複数人登録できる仕組み（`adminLineUsers`）を実装済み。休みの人の代理が別のLINEアカウントで通知受信・受け取り確認の両方を行える。

**4.2 【A】管理画面のログインは全管理者共通の1パスワード**
問題：個人を特定できないため、サイト経由での支払い確認は「誰が確認したか」の記録が残らない（LINE経由の`confirmedBy`のみ記録される）。
改善案：個人特定が必要な場合は将来的なアカウント制移行が必要（大掛かりなため今回は対象外）。

**4.3 【B】通知オフのまま長期間気づかない可能性**
問題：「新規注文ごとに通知」をオフにしたまま忘れると新規注文の把握が遅れる。支払い申告の通知は別ロジックで常に届くため完全な見落としにはなりにくい。

**4.4 【A】通知0件と未設定の区別がつきにくい**
問題：LINE未登録の状態では通知が一切来ないが、「正常に0件」なのか「設定漏れ」なのか管理者からは区別しづらい。設定手順書の動作確認チェックリストで最初に必ず自分のLINEを登録するよう明記済み。

**4.5 【B】複数管理者間の責任分散（バイスタンダー効果）**
問題：全員に同じ通知が届くため「誰かがやるだろう」と様子見される可能性。システムでは解決できない運用課題。「対応したら一言報告する」等のルール化を推奨。

**4.6 【C】設定保存忘れ**
問題：保存ボタンを押さず離脱すると変更が破棄される。実害は「入力し直すだけ」で小さい。

**4.7 【B】管理セッションは30日間有効（既存仕様）**
確認：共用端末でログインしたままだと部外者が操作できる可能性。新設したLINE通知先の登録・解除も同じセッションで可能なため、共有端末利用時の運用注意点として明記。

**4.8 【C】管理者は他の管理者のLINE登録を解除できる**
確認：`isAdminRequest()`のみのチェックで対象が自分自身かは区別していないが、これは「複数管理者で互いに管理し合う」という意図した設計として妥当。

**4.9 【C】登録済みLINE一覧の表示名だけでは端末を区別しにくい**
問題：表示名が同じ・空の場合に区別しづらい。登録日時の併記があるとより親切（今回は表示名のみ）。

**4.10 【S】チャネルアクセストークン漏えい時のリスク**
確認：Webhook署名検証・管理者ID再検証により、トークンさえ漏れなければ第三者はなりすまし操作ができない設計。トークン自体の管理（Vercel環境変数への保存）は運用者の注意に依存するため、手順書で強く注意喚起済み。

---

## 5. LINE通知漏れ・通知が多すぎる問題・通知先の間違い・LINE連携全般

**5.1 【S】LINEアプリ外からのアクセスでは通知が届かない**
確認：LIFFはLINEアプリ内ブラウザで開いた場合のみ確実に自動ログインする。PCブラウザ等ではLINEユーザーIDが取得できず通知が届かない（注文自体は問題なくできる）。リッチメニュー経由での利用を前提とする旨を手順書に明記済み。

**5.2 【S】同姓同名によるLINE通知の誤送信**
問題：名前をキーにLINEユーザーIDを紐付ける設計のため、同姓同名の別人が同じ名前で注文すると後から開いた方のLINEアカウントで上書きされ、先に登録した人への通知（支払い確認など金銭に関わる通知を含む）が届かなくなる、または別人に届く。
改善案：現場に同姓同名がいる場合は名前管理で「山田太郎(A班)」のように区別できる表記にすることを強く推奨。README・設定手順書に明記。

**5.3 【A】繁忙時間帯の通知過多**
問題：「新規注文ごとに通知」をオンのままだと6:30〜7:55に70人分の通知が連続し、重要な支払い申告通知が埋もれるリスク。
改善：管理画面にオン/オフ切替を実装済み（デフォルトは要件通りオン）。将来的な「まとめ通知」への拡張も可能。

**5.4 【B】LINEメッセージ配信数の無料枠超過リスク**
問題：70人×毎日複数回の通知は、LINE公式アカウントの無料プランの月間配信数上限を超える可能性がある。運用開始後にLINE Official Account Managerで配信数を確認することを推奨。

**5.5 【B】Webhook障害時のフォールバック**
確認：Webhookが一時的に届かない場合でも、管理画面から直接「受け取り確認（支払い確定）」操作ができるため、機能停止にはならない。

**5.6 【A】明日の締切が朝のリマインドに含まれない**
問題：6:30のリマインドは今日の注文のみ言及し、明日の締切（15:00）には触れないため、利用者が明日の注文を頼み忘れる可能性。
改善案：必要であれば文言に追記可能（今回は要件通りの内容で実装）。

**5.7 【C】サイトURL変更時にLIFF側の更新が必要**
確認：独自ドメイン化等でURLが変わった場合、LIFFのエンドポイントURL設定も更新が必要である旨を手順書に追記済み。

**5.8 【C】LIFF IDは公開されても問題ない設計**
確認：`NEXT_PUBLIC_LIFF_ID`はクライアントに公開される値だが、LINEの仕組み上これ自体の漏えいは実害がない（本人確認はLINE側で行われるため）。

**5.9 【B】「注文0件」の日も毎日通知が届く**
確認：休工日が多い現場では「注文はありませんでした」という通知が続き、慣れによって通知を見落とす習慣がつく可能性（オオカミ少年効果）。0件の日は通知を省略する設計も検討可能だが、「未設定で届いていないだけ」との区別がつかなくなるトレードオフがあるため、今回は毎日必ず届く設計を維持。

**5.10 【C】受け取り確認ボタンの誤タップ対策**
確認：Flexメッセージのボタンは「✅受け取り確認」のみで、誤って別の注文のボタンを押しても取り消しはサイト側で可能。

**5.11 【C】実機でのFlexメッセージ表示確認は未実施**
確認：LINE公式アカウントの実環境がないため、Flexメッセージの見た目確認はこのセッション内では行えていない。LINE公式の標準的なbubble構造に準拠したコードにはなっている。本番投入前に実機確認を推奨。

**5.12 【B】Vercelデプロイ失敗時はCronも止まる**
確認：アプリ本体とCronが同一デプロイのため、デプロイ失敗中は定時通知も動かない。Vercel側のデプロイ失敗通知設定は別途利用者側で行う必要がある。

**5.13 【S】CRON_SECRET漏えい時のリスク**
確認：漏えいすると第三者が定時通知を偽装トリガーできてしまう（実害は誤情報配信で、金銭操作は不可）。ランダムな文字列での厳重管理を手順書に明記済み。

**5.14 【A】複数現場での使い回しに関する注意**
問題：本システムは1現場＝1公式LINEアカウントを前提とした設計。複数現場を掛け持ちする場合は環境変数を現場ごとに完全分離する必要がある。

---

## 6. 同姓同名・注文者が休みになった場合

**6.1 【S】同姓同名による注文の誤上書き**
問題：Aさんの「山田太郎」が注文済みの状態で、別人のBさん「山田太郎」が注文しようとすると、既存注文が「自分の注文」として表示され、上書きしてしまう可能性がある（5.2と表裏一体）。
改善案：名前管理での区別表記の徹底を最優先の運用対策として推奨。システム側での自動判別は追加の個人識別情報がない限り困難。

**6.2 【A】注文者が急に休みになった場合の対応**
問題：前日払い済みの状態で欠勤が決まった場合、締切後は管理者のみが削除可能。返金は人力対応。

**6.3 【B】名前削除後も過去の注文履歴には名前が残る**
確認：意図した挙動（履歴は改変しない）。

**6.4 【C】未使用の登録名がドロップダウンに溜まる**
問題：長期的に名前選択の候補が増え続け選びにくくなる。定期整理を推奨。

**6.5 【A】退職者・長期休職者の名前が選択候補に残るリスク**
問題：同姓同名と同様、別の人が誤って選んでしまうリスクがある。異動・退職時のフローに名前削除を組み込むことを推奨。

**6.6 【C】「休み」自体をシステムに伝える手段はない**
確認：本システムは注文アプリであり勤怠管理は対象外（想定通り）。

**6.7 【S】同姓同名によるLINE通知誤送信の金銭的インパクト**
確認：6.1・5.2の帰結として、支払い確認等の金銭が絡む通知が誤った相手に届く可能性がある点を最重要リスクとして改めて強調。

**6.8 【C】名前の文字数上限（20文字）**
確認：区別のための追記（班名等）をすると上限に達する可能性。必要であれば3ファイルの上限値を変更するだけで対応可能。

---

## 7. 急な休工・弁当屋の休み・メニュー変更

**7.1 【A】休工の一斉告知機能はシステムにない**
確認：LINE公式アカウントマネージャーの手動一斉送信機能で代替可能。システム機能としては未実装（今回の要件外）。

**7.2 【B】メニュー0件と「休業」の区別がつかない**
問題：登録忘れなのか意図的な休業なのか利用者に伝わらない。「本日休業」フラグ等の追加は今後の拡張候補。

**7.3 【C】同日内の価格変更**
確認：注文時点の単価をスナップショット保存する設計により、同じ日でも人によって単価が異なる状態が起こり得るが、これは意図した正しい動作。

**7.4 【A】メニュー名変更後、既存注文の「変更」時に選択肢が一致しない**
確認：既存仕様（今回変更なし）。実害は「選び直すだけ」だが初見だと戸惑う可能性。

**7.5 【B】削除済みメニューが集計に残り続ける**
確認：過去の正確な記録として意図的に残る設計。管理者が混乱しないよう認識共有が必要。

**7.6 【S】弁当屋休業時の「臨時停止」手段がない**
問題：締切時刻を早める以外に注文を止める手段がなく、元に戻し忘れると翌日以降も影響する。
改善案：「本日は臨時休止」の一時停止スイッチの追加を今後の検討候補として推奨（今回は要件外）。

**7.7 【C】メニュー追加とリマインド送信のタイミング**
確認：ページは`force-dynamic`で都度再生成されるため、直前のメニュー追加でも反映漏れの心配はない（検証済み・問題なし）。

**7.8 【C】大盛り以外のバリエーションには対応していない**
確認：既存仕様どおり（今回のスコープ外）。

---

## 8. スマホ操作・ITが苦手な人

**8.1 【A】LINEログイン確認画面での不安**
確認：失敗しても注文自体は可能なため実害は小さいが、初見の利用者向けの周知が有効。

**8.2 【B】「申告済み・確認待ち」の意味の伝わりやすさ**
確認：補足テキストを併記済みで一定配慮済み。

**8.3 【C】「変更」ボタンの文言が2箇所で紛らわしい**
確認：名前の変更とメニューの変更で用途が異なるが、初見では混同しうる。軽微。

**8.4 【A】エラーメッセージの分かりやすさ**
確認：平易な日本語で表示されており一定配慮済み。

**8.5 【B】キャンセル確認ダイアログの誤タップリスク**
確認：ブラウザ標準の`confirm()`はスマホでは見た目が小さい場合がある。

**8.6 【C】タップ領域は既存デザインシステムで十分確保**
確認：良好な既存設計（変更不要）。

**8.7 【A】極端に古い端末・ブラウザでのLIFF非対応**
確認：try/catchで安全に握りつぶす設計のため、エラーで画面が壊れることはなく、注文機能自体は維持される。

**8.8 【C】大盛り追加料金の実感しやすさ**
確認：合計金額のリアルタイム表示で配慮済み。

**8.9 【B】ブラウザバック時の挙動は未検証**
確認：Next.js標準のルーティングに従うため大きな問題は想定しにくいが、実機での網羅的検証は未実施。

**8.10 【A】手袋をしたままの操作**
確認：一般利用者向け画面は大きめボタンで統一済み。管理画面側の小さいボタン（LINE解除等）は優先度中。

---

## 9. 通信障害

**9.1 【S】送信中の通信断で「失敗」と誤解し再送信するケース**
確認：`upsertOrder`は同一人物・同一日付なら上書きする設計のため、再送信しても二重注文にはならない（良好な既存設計）。

**9.2 【A】支払い関連操作も同様に冪等**
確認：`claimOrderPaid`・`setOrderPaymentStatus`ともに再実行しても多重処理・多重通知が起きない設計であることを確認済み。

**9.3 【B】LINE通知自体の遅延・不達**
確認：LINE側の配信保証範囲内の話でシステムでは制御不可。「サイト＝正確な最新情報、LINE＝通知のきっかけ」という役割分担のため、LINEが届かなくてもサイトで正しい状態を確認できる。

**9.4 【C】オフライン専用画面は未対応**
確認：今回のスコープ外。

**9.5 【A】Webhook瞬断時のフォールバック**
確認：管理画面から直接確定できる代替手段が常に存在する。

**9.6 【B】Cron実行時の一時障害でその回の通知がスキップされる**
確認：リトライ機構は未実装。LINEに完全依存しない設計方針により、管理画面での定期確認が引き続き有効な保険となる。

---

## 10. セキュリティ

**10.1 【S】〈実装済み〉Webhook署名検証**
確認：HMAC-SHA256による署名検証を必須化し、検証失敗時は401で拒否。

**10.2 【S】〈実装済み〉管理者postbackの再検証**
確認：受け取り確認の送信者IDをサーバー側で必ず`adminLineUsers`と照合。

**10.3 【S】〈実装済み〉Cronエンドポイントの認証**
確認：`CRON_SECRET`の完全一致を要求、未設定時は常に拒否。

**10.4 【A】注文・支払い申告・LINE紐付けAPIは無認証（名前ベース）**
確認：これはアカウント制を敷かない既存の設計思想に起因するもので、今回新たに生まれたリスクではない。URLを知る人なら誰でも操作できる設計であることは留意点として明記。

**10.5 【B】レート制限は未実装**
確認：現場内限定の利用という前提でリスクは低いが、追加のレート制限は今回のスコープ外。

**10.6 【A】初期パスワード`admin123`のまま運用するリスク**
確認：既存の注意事項だが、支払い確定・LINE設定変更も同じ認証に相乗りしたため重要性が増している。必ず変更することを改めて強調。

**10.7 【B】チャネルアクセストークン漏えい時の全体リスク**
確認：漏えいするとLINE公式アカウント全体を乗っ取られるのと同義。Vercel環境変数での厳重管理を手順書で強調済み。

**10.8 【C】`.env.example`に実値は含まれない**
確認：良好な既存設計を維持。

**10.9 【A】管理セッションCookieの属性は今回のスコープ外**
確認：`lib/auth.ts`のHMAC署名トークン自体は健全だが、Cookie発行箇所（`app/api/admin/login/route.ts`）の`HttpOnly`等の属性は今回変更していないため未確認。次回対応候補。

**10.10 【C】LIFFスコープは`profile`のみに最小化**
確認：メールアドレス等の追加スコープは要求しない設計で、プライバシーに配慮済み。

---

## 11. データ消失・誤操作・注文削除

**11.1 【S】〈対応済み〉Redis同時書き込みでのデータ消失（1.1と同一）**
確認：簡易ロック機構を追加し修正済み。

**11.2 【A】削除操作にUndo機能がない**
確認：既存仕様。重要操作である旨の注意喚起強化の余地あり。

**11.3 【B】データは自動削除されず容量が増え続ける**
確認：現状の規模なら数年単位で問題にならない見込み。

**11.4 【S】Upstash無料枠の上限超過リスク**
問題：70人が毎日使うと、通知APIコールを含めコマンド数が増加し、将来的に無料枠を超過する可能性がある。運用開始後、Upstashダッシュボードでの定期確認を推奨（自動監視は未実装）。

**11.5 【確認・問題なし】ローカルDBファイルはGit管理外**
確認：`.gitignore`に`data/db`が含まれており、テスト用の個人名等がリポジトリにコミットされる心配はない（既存の良好な設計を確認）。

**11.6 【A】LINEユーザーIDと実名の紐付けデータの機微性**
問題：`lineLinks`（名前⇔LINEユーザーID）は通常の注文データより機微性が高い個人情報。保存先へのアクセス権限を環境変数と同水準で厳重管理する必要がある旨を追記推奨。

**11.7 【C】画像データと重要データが同一Redisインスタンス**
確認：既存設計（今回の変更範囲外）。

**11.8 【B】支払い取り消し時のタイムスタンプの残留**
問題：`paid→unpaid`に取り消した際、`paymentConfirmedAt`は明示的にクリアされない。次回確定時に上書きされるため実害はない。

**11.9 【B】注文削除に理由記録機能がない**
確認：監査ログ機能は今回のスコープ外。

**11.10 【C】バックアップ機能はシステムにない**
確認：Upstash側のプラン依存機能、または手動運用に委ねられる（今回のスコープ外）。

---

## 12. その他運用（通知先の間違いなど）

**12.1 【S】複数現場運用時の環境変数混同リスク**
確認：1現場1公式LINEアカウントを前提とした設計。複数現場を掛け持ちする場合は環境変数の完全分離が必須。

**12.2 【A】共用端末での誤ったLINEアカウント登録**
問題：共用端末で他人のLINEアプリにログインしたまま管理者登録操作をすると、他人のLINEが管理者として登録されてしまう可能性。

**12.3 【B】「担当者名」と「LINE通知の受け取り先」の混同**
確認：別カードで明確に分離して表示済み。

**12.4 【C】会社名での注文運用時の混乱**
確認：個人名での運用を徹底することを推奨（会社名だと支払い確認・LINE通知の紐付けが機能しにくい）。

**12.5 【A】現金ケース廃止直後の利用者の戸惑い**
改善案：リッチメニューのあいさつメッセージやトップページで「支払い方法が変わりました」の一時告知を出すと親切（システム機能ではなく運用上の周知事項）。

**12.6 【B】前日払い制移行期の注文し忘れ**
改善案：運用開始から数日は管理者による個別フォローを推奨。

**12.7 【S】LINE実機での疎通確認は未実施**
確認：環境変数が未設定のため、このセッション内ではLINE公式アカウント・LIFF・Messaging APIとの実機疎通確認ができていない。本番投入前に設定手順書のチェックリストに沿った実機確認を強く推奨。

**12.8 【B】Vercel Hobbyプランでのcron実行時刻の誤差**
確認：指定時刻ちょうどではなく数分前後する場合がある（Vercelの仕様）。実用上は問題ない前提。

---

## まとめ

- 合計 **120件** の観点を洗い出しました（目標100件以上）。
- このうち **6件は今回のセッション内でコードを修正して対応済み**です：
  1. Redisバックエンドへの排他制御（ロック）追加（注文・支払い・設定・LINE紐付けの同時書き込みによるデータ消失を防止）
  2. 注文の同一人物判定ロジックの不整合修正（二重注文の防止）
  3. 複数管理者のLINE通知先登録機能
  4. 金額変更時の支払い状況引き継ぎに関する管理者向け警告表示
  5. 新規注文通知のオン/オフ切替
  6. 管理画面での支払い申告・確認時刻の可視化（トラブル時の客観的記録）

- **重要度Sで未対応・要検討**のものは主に以下の3点で、次回以降の対応候補として優先度が高いです。
  1. 支払い済み注文の自己キャンセルに対する追加ガード（1.3）
  2. 締切時刻設定とVercel Cron実行時刻の連動（2.1）
  3. 同姓同名によるLINE通知の誤送信リスク（5.2 / 6.1 / 6.7）— これは運用ルール（名前の区別表記）での対応が現実的です

- 残りの多くは「既存の設計として妥当」「運用ルールで対応すべき事項」「軽微なUX改善候補」に分類されます。
